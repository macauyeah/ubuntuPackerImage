# This template requires Lima v0.7.0 or later.
images:
# Fallback to the latest release image.
# Hint: run `limactl prune` to invalidate the cache
- location: "file://ABSOLUTE_PATH_TO_YOU_IMAGE/output-autogenerated_1/packer-autogenerated_1"
  arch: "x86_64"

mounts:
- location: "~"
- location: "/tmp/lima"
  writable: true
